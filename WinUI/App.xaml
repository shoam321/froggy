<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="BluetoothWidget.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BluetoothWidget">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <ResourceDictionary Source="ms-appx:///Controls/BatteryRing.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Custom Acrylic Brush for Cards -->
            <AcrylicBrush x:Key="CardAcrylicBrush"
                          TintColor="{ThemeResource SystemAltHighColor}"
                          TintOpacity="0.6"
                          FallbackColor="{ThemeResource SystemAltMediumHighColor}" />

            <!-- Battery Gradient Stops -->
            <LinearGradientBrush x:Key="BatteryHighBrush" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#00BFA5" Offset="0"/>
                <GradientStop Color="#1DE9B6" Offset="1"/>
            </LinearGradientBrush>
            
            <LinearGradientBrush x:Key="BatteryMediumBrush" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FFB300" Offset="0"/>
                <GradientStop Color="#FFC107" Offset="1"/>
            </LinearGradientBrush>
            
            <LinearGradientBrush x:Key="BatteryLowBrush" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#E53935" Offset="0"/>
                <GradientStop Color="#FF5252" Offset="1"/>
            </LinearGradientBrush>

            <!-- Card Style -->
            <Style x:Key="DeviceCardStyle" TargetType="Border">
                <Setter Property="Background" Value="{ThemeResource CardAcrylicBrush}"/>
                <Setter Property="CornerRadius" Value="12"/>
                <Setter Property="Padding" Value="16"/>
                <Setter Property="BorderBrush" Value="{ThemeResource ControlElevationBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
